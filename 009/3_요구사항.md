Q: 지금 이 폴더중 008번 폴더에 requirements.txt 파일이 없어. 이 파일을 생성하는 명령어가 어떻게 되지? 
A: pip freeze > requirements.txt

Q: 이 문서를 기반으로 설치를 하고 싶으면 어떤 명령어가 필요하지?
A: python -m venv venv
   source venv/bin/activate  # Linux/Mac
   venv\Scripts\activate     # Windows
A: pip install -r requirements.txt

Q: 이 프로젝트의 배포는 render라는 곳에서 할거야. render라는 서비스에서는 requirements.txt 라는 파일이 있으면 이 파일을 기반으로 내부에 설치해서 배포해. 지금 우리 폴더에는 requirements.txt가 있어. render 서비스에서 배포할 때 처음에 `gunicorn 프로젝트명.wsgi`으로 시작해.

이렇게 render 서비스에 배포하기 위한 작업을 `배포.md`파일에 정리해줘. 지금 프로젝트는 `1_명령어.md` 파일에 모든 명령어가 정리되어 있어.