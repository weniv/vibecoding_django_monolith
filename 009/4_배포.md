# Render 배포 가이드

이 문서는 Django 블로그 프로젝트를 Render 서비스에 배포하는 방법을 안내합니다.

---

## 목차
1. [배포 준비](#배포-준비)
2. [Render 설정](#render-설정)
3. [배포 후 확인](#배포-후-확인)
4. [문제 해결](#문제-해결)

---

## 배포 준비

### 1. requirements.txt 확인
Render는 `requirements.txt` 파일을 기반으로 필요한 패키지를 자동으로 설치합니다.

현재 프로젝트의 `requirements.txt` 파일에 다음 패키지들이 포함되어 있는지 확인하세요:

```txt
django
markdown
Pillow
gunicorn
```

**gunicorn 추가 설치 (로컬 환경에서)**:
```bash
pip install gunicorn
pip freeze > requirements.txt
```

### 2. settings.py 배포 설정

* 그대로 두셔도 됩니다! 우리는 테스트 하는 것이니까요!

**파일**: `config/settings.py`

배포를 위해 다음 설정을 추가/수정해야 합니다:

```python
# config/settings.py

import os

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.environ.get('DEBUG', 'False') == 'True'

# 호스트 설정
ALLOWED_HOSTS = ['*']  # 또는 실제 도메인명으로 변경

# Database
# Render는 PostgreSQL을 권장하지만, SQLite로도 시작 가능
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}

# Static files
STATIC_URL = '/static/'
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'static'),
]
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

# Media files
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```

### 3. .gitignore 파일 생성 (선택사항)

배포 전 민감한 정보나 불필요한 파일을 제외하기 위해 `.gitignore` 파일을 생성합니다:

```gitignore
# Python
*.pyc
__pycache__/
*.py[cod]
*$py.class

# Django
*.log
db.sqlite3
db.sqlite3-journal
/media
/staticfiles

# Virtual Environment
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
```

* 아래와 같이 간단하게 작성하겠습니다.

```
# Django
# db.sqlite3 # 기존에 DB를 사용할 것이면 이렇게 주석처리 해주세요.
# db.sqlite3-journal # 기존에 DB를 사용할 것이면 이렇게 주석처리 해주세요.

# Virtual Environment
venv/
```

### 4. 정적 파일 수집

* 안하셔도 됩니다!

로컬에서 정적 파일을 미리 수집해봅니다:

```bash
python manage.py collectstatic --noinput
```

---

## Render 설정

### 1. GitHub에 코드 업로드

Render는 GitHub 저장소와 연동하여 배포합니다. 먼저 GitHub에 프로젝트를 업로드하세요.

```bash
# Git 초기화 (아직 안했다면)
git init

# 모든 파일 추가
git add .

# 커밋
git commit -m "Initial commit for Render deployment"

# GitHub 저장소와 연결
git remote add origin https://github.com/your-username/your-repo.git

# 푸시
git push -u origin main
```

### 2. Render 웹 서비스 생성

1. **Render 계정 생성**
   - https://render.com 접속
   - GitHub 계정으로 로그인

2. **New Web Service 생성**
   - Dashboard에서 "New +" 버튼 클릭
   - "Web Service" 선택
   - GitHub 저장소 연결 및 선택

3. **기본 설정**
   - **Name**: 원하는 서비스 이름 입력 (예: `django-blog`)
   - **Region**: 가장 가까운 지역 선택 (예: `Singapore`)
   - **Branch**: `main` (또는 배포할 브랜치)
   - **Root Directory**: 비워두기 (또는 프로젝트 루트 경로)
   - **Runtime**: `Python 3`

4. **Build & Start Commands 설정**

   **Build Command** (빌드 시 실행):
   ```bash
   pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
   ```

   **Start Command** (서버 시작):
   ```bash
   gunicorn config.wsgi
   ```

5. **Environment Variables 설정**

   "Environment" 탭에서 다음 환경 변수를 추가합니다:

   | Key | Value | 설명 |
   |-----|-------|------|
   | `PYTHON_VERSION` | `3.11.0` | Python 버전 지정 |
   | `SECRET_KEY` | `your-secret-key-here` | Django SECRET_KEY |
   | `DEBUG` | `False` | 배포 환경에서는 False |

   **SECRET_KEY 생성 방법**:
   ```python
   # Python 콘솔에서 실행
   from django.core.management.utils import get_random_secret_key
   print(get_random_secret_key())
   ```

6. **Instance Type 선택**
   - Free 플랜 선택 (처음 사용시)

7. **Create Web Service 클릭**

### 3. 배포 진행

- Render가 자동으로 빌드 및 배포를 시작합니다.
- 로그를 확인하여 배포 진행 상황을 모니터링할 수 있습니다.
- 배포가 완료되면 제공된 URL로 접속할 수 있습니다.

---

## 배포 후 확인

### 1. 관리자 계정 생성

배포 후 Shell 탭에서 관리자 계정을 생성합니다:

1. Render Dashboard에서 서비스 선택
2. "Shell" 탭 클릭
3. 다음 명령어 실행:

```bash
python manage.py createsuperuser
```

### 2. 테스트

- 배포된 URL에 접속하여 정상 작동 확인
- 관리자 페이지 접속: `https://your-app.onrender.com/admin`
- 회원가입, 로그인, 게시글 작성 등 주요 기능 테스트

---

## 문제 해결

### 1. Static 파일이 로드되지 않는 경우

**문제**: CSS, JavaScript 등 정적 파일이 제대로 로드되지 않음

**해결방법**:
- `config/settings.py`에서 `STATIC_ROOT` 설정 확인
- Build Command에 `python manage.py collectstatic --noinput` 포함 확인

### 2. Database 마이그레이션 오류

**문제**: 배포 후 데이터베이스 관련 오류 발생

**해결방법**:
- Build Command에 `python manage.py migrate` 포함 확인
- Shell에서 수동으로 마이그레이션 실행:
  ```bash
  python manage.py migrate
  ```

### 3. gunicorn 실행 오류

**문제**: `gunicorn config.wsgi` 명령어 실행 실패

**해결방법**:
- `requirements.txt`에 `gunicorn` 포함 확인
- Start Command가 `gunicorn config.wsgi`로 정확히 입력되었는지 확인

### 4. ALLOWED_HOSTS 오류

**문제**: "DisallowedHost" 오류 발생

**해결방법**:
- `config/settings.py`의 `ALLOWED_HOSTS`에 Render 도메인 추가:
  ```python
  ALLOWED_HOSTS = ['your-app.onrender.com', 'localhost', '127.0.0.1']
  ```

### 5. Media 파일 업로드 문제

**문제**: 이미지 업로드가 작동하지 않음

**참고**: Render Free 플랜은 파일 시스템이 임시적이므로 업로드된 파일이 재배포 시 삭제될 수 있습니다.

**해결방법**:
- 프로덕션 환경에서는 AWS S3, Cloudinary 등 외부 스토리지 사용 권장
- 또는 Render의 Persistent Disk 기능 사용 (유료)

---

## 추가 정보

### Render 자동 배포

GitHub 저장소의 지정된 브랜치에 새로운 커밋이 푸시되면 Render가 자동으로 재배포합니다.

### 로그 확인

- Render Dashboard > 서비스 선택 > "Logs" 탭에서 실시간 로그 확인 가능

### 환경 변수 수정

- 환경 변수 변경 시 서비스가 자동으로 재시작됩니다.

---

## 요약

1. **준비**: `requirements.txt`에 `gunicorn` 추가
2. **GitHub**: 코드를 GitHub에 업로드
3. **Render**: Web Service 생성 및 설정
   - Build Command: `pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate`
   - Start Command: `gunicorn config.wsgi`
4. **배포**: Render가 자동으로 빌드 및 배포
5. **확인**: 배포된 사이트 접속 및 테스트

배포가 완료되었습니다! 🎉

---

정적 파일이 깨질 경우 아래 명령어로 해결 가능


```
# config/urls.py
# 정적 파일 서빙 (개발 환경에서만)
if settings.DEBUG:
    urlpatterns += static(settings.STATIC_URL, document_root=settings.STATIC_ROOT)
```

```
python manage.py collectstatic --noinput
```
