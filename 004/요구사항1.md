나는 Django를 이제 막 배우는 초보 개발자야. 강사님이 `명령어_백업.md` 파일을 이전시간에 작성하여 주었어.

이 파일을 참고해서 실제 동작하는 Django 웹 애플리케이션을 만들어 보고 싶어. 지금 이 파일은 `.html`파일이 아니라 `httpResponse`로 응답을 주는 간단한 뷰 함수로 작성되어 있는데 `templates` 폴더를 만들어서 HTML 파일로 응답을 주는 방식으로 바꾸고 싶어. 

이러한 방식을 `명령어.md` 파일로 만들고 작성해줘.